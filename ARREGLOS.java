package practica1
public class ARREGLOS {
import java.util.*;
public static void main (String [] args) {
	int[] numeros = new int[15];
	int num;
	Scanner captura = new Scanner (System.in);
	for(int x = 0; x < numeros.length; x++) {
	System.out.println("dame el valor:" +x);
	num = captura.nextInt();
	numeros[x] = num;
}
	for(int i = 0; i <numeros.length; i ++) {
	//string n = numero [i] %2 == 0? "hola" : "no"	
		if(numeros[i] %2 == 0) {
			double raiz;
			raiz =Math.sqrt(numeros[i] > 0) {
			System.out.println("la raiz cuadradade" + numero[x]+" " raiz);
		if(numeros[i] < 0) {
			
			contador_neg += 1;
		}
		}
	}
}
}
}